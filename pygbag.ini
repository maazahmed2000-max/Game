; pygbag packager — keep web build and CI metadata out of the .apk payload
[DEPENDENCIES]
ignoredirs = [".github", "build", "server"]
ignorefiles = ["build_web.ps1", ".gitignore", "README.md", "push_to_github.ps1", "complete_github.ps1", "Procfile"]
